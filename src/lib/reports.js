// Общие константы отчётов: цвета бейджей, редактируемые статусы и объяснения
// для замороженных. Вынесены из компонентов, потому что нужны сразу в трёх
// местах (список отчётов, детали отчёта, шторка «Прикрепить» в карточке чека),
// а расхождение хотя бы в одной строке тихо ломает бейдж или разрешает
// действие, которое бэк отвергнет.
//
// Источник истины по статусам — бэкенд: Literal["Черновик","На проверке",
// "Одобрен","Отклонён"] в app/routers/reports.py.

// Пилюли-бейджи статусов — цвета из макета templates/reports/Отчёты.html.
export const BADGE = {
  Черновик: { bg: "#EEF0F4", color: "#636B7D" },
  "На проверке": { bg: "#FFFBEB", color: "#B45309" },
  Одобрен: { bg: "#F0FDF4", color: "#15803D" },
  Отклонён: { bg: "#FCEBEB", color: "#B91C1C" },
};

// Статусы, в которых бэк разрешает менять отчёт: удалять и править состав
// (EDITABLE_STATUSES в app/routers/reports.py). В остальных придёт 409.
export const EDITABLE_STATUSES = ["Черновик", "Отклонён"];

export const isEditable = (status) => EDITABLE_STATUSES.includes(status);

// Кто принимает решение по отчёту. Зеркало can_see_all на бэке
// (app/routers/reports.py: _require_approver → 403 «Одобрять и отклонять
// отчёты может только бухгалтер или администратор»).
// ВАЖНО: это не защита, а только вид. Защита — на бэкенде; фронт лишь не
// показывает кнопку, которая гарантированно вернёт 403.
// role === null значит «ещё не загрузилась» (первые кадры до /api/users/me),
// а НЕ «прав нет» — поэтому проверка строгая по значению, без приведения
// к булеву: иначе неизвестность читалась бы как отказ.
export const canApprove = (role) => role === "admin" || role === "accountant";

// Почему в этом статусе ничего нельзя — и что делать дальше. Показываем
// строкой под бейджем вместо «мёртвых» disabled-кнопок: в них тыкают и не
// понимают причину. Тексты согласованы с 409-ответами бэка.
export const FROZEN_HINT = {
  "На проверке":
    "Отчёт у проверяющего: состав и удаление заморожены. Отзовите, чтобы изменить",
  Одобрен: "Отчёт принят к учёту: изменения недоступны",
};
